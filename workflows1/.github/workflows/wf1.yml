name: Workflow 1

on:
  workflow_dispatch:

jobs:
  mi-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Hola
        run: echo "hola"